开发一款仿 Apple Music 风格的 Android 音乐播放器，并结合你维护的 `nimabi520/api-enhanced` (网易云音乐 API) 作为后端，是一个非常棒的项目！

更新：可以通过这个库实现liquid glass效果  https://github.com/Kyant0/AndroidLiquidGlass?tab=readme-ov-file
并且歌词界面可以参考：https://github.com/Yos-X/FlamingoSank/ 还有 https://github.com/amll-dev/applemusic-like-lyrics

为了实现类似 Apple Music 那样流畅、现代化的 UI 和稳定的后台播放体验，强烈建议使用 **现代 Android 开发技术栈**：
*   **开发语言**：Kotlin
*   **UI 框架**：Jetpack Compose (非常适合实现 Apple Music 复杂的交互、高斯模糊和流畅动画)
*   **架构模式**：MVVM (Model-View-ViewModel) + 响应式架构 (MVI/Clean Architecture)
*   **网络请求**：Retrofit + OkHttp + Kotlin Coroutines & Flow
*   **媒体播放**：AndroidX Media3 (ExoPlayer) - 现代 Android 音频播放的标准库
*   **依赖注入**：Hilt

以下是推荐的 Android 项目目录结构，采用了基于功能（Feature-based）和分层（Layered）的混合架构设计：

```text
app/src/main/java/com/example/seteasecloudmusic/
│
├── App.kt                           // Application 全局类，初始化 Hilt 等
│
├── di/                              // Dependency Injection (Hilt 依赖注入模块)
│   ├── NetworkModule.kt             // 提供 Retrofit 实例 (连接你的 Node.js API)
│   ├── PlayerModule.kt              // 提供 Media3 Player 实例
│   ├── RepositoryModule.kt          // 提供数据仓库实例
│   ├── AuthModule.kt                // 提供认证相关依赖
│   └── SessionModule.kt             // 提供会话持久化依赖
│
├── data/                            // 数据层 (Data Layer)
│   ├── api/                         // 网络请求接口
│   │   ├── NeteaseMusicService.kt   // 音乐相关接口（搜索/歌单/歌曲URL等）
│   │   ├── auth/
│   │   │   └── AuthService.kt       // 登录相关接口（手机/邮箱/验证码/二维码/游客/刷新）
│   │   └── interceptor/
│   │       └── AuthInterceptor.kt   // 统一注入 Cookie、处理鉴权失败重试
│   ├── local/
│   │   └── session/
│   │       ├── CookieStore.kt       // 仅存取 Cookie/MUSIC_U
│   │       └── SessionLocalDataSource.kt
│   ├── model/                       // 数据模型 (DTOs)
│   │   ├── SongDao.kt               // API 返回的歌曲原始数据
│   │   ├── PlaylistDao.kt           // 歌单数据
│   │   ├── SearchResultDao.kt
│   │   └── auth/
│   │       ├── LoginResponseDto.kt
│   │       ├── QrLoginDto.kt
│   │       └── RefreshResponseDto.kt
│   └── repository/                  // 数据仓库实现
│       ├── MusicRepositoryImpl.kt   // 处理网络数据与本地数据的合并
│       └── AuthRepositoryImpl.kt    // 处理认证接口 + 本地会话存储
│
├── domain/                          // 领域层 (Domain Layer - 核心业务逻辑)
│   ├── model/                       // App 内部使用的纯净数据模型
│   │   ├── Track.kt                 // 标准化歌曲模型 (ID, 名称, 歌手, 封面 URL, 播放 URL)
│   │   ├── Album.kt
│   │   └── auth/
│   │       ├── AuthSession.kt
│   │       ├── LoginMethod.kt
│   │       └── QrStatus.kt
│   ├── repository/                  // 数据仓库接口定义
│   │   ├── MusicRepository.kt
│   │   └── AuthRepository.kt
│   └── usecase/                     // 具体的业务逻辑用例
│       ├── GetDailyRecommendationsUseCase.kt
│       ├── SearchMusicUseCase.kt
│       └── auth/
│           ├── PhoneLoginUseCase.kt
│           ├── EmailLoginUseCase.kt
│           ├── SendCaptchaUseCase.kt
│           ├── VerifyCaptchaUseCase.kt
│           ├── StartQrLoginUseCase.kt
│           ├── PollQrStatusUseCase.kt
│           ├── GuestLoginUseCase.kt
│           ├── RefreshSessionUseCase.kt
│           └── ObserveAuthStateUseCase.kt
│
├── player/                          // 播放器服务层 (核心功能)
│   ├── playback/
│   │   ├── MusicService.kt          // 继承自 Media3 的 MediaLibraryService (后台播放核心)
│   │   └── MusicPlayerController.kt // 封装 MediaController，供 ViewModel 调用
│   └── notification/                // 自定义通知栏播放控制 (可选，Media3 会自动生成)
│
├── presentation/                    // 表现层 (UI Layer - Jetpack Compose)
│   ├── MainActivity.kt              // 单 Activity 架构入口
│   ├── navigation/                  // Compose 导航路由
│   │   └── AppNavigation.kt
│   ├── theme/                       // Apple Music 风格的主题设计
│   │   ├── Color.kt                 // 标志性的红色强调色和深色/浅色模式适配
│   │   ├── Theme.kt
│   │   └── Type.kt                  // 字体排版设置 (大标题风格)
│   │
│   ├── components/                  // 全局复用 UI 组件
│   │   ├── AppleMusicBottomBar.kt   // 底部导航栏
│   │   ├── TrackListItem.kt         // 歌曲列表项
│   │   ├── AlbumCover.kt            // 带有圆角和阴影的专辑封面
│   │   └── BlurBackground.kt        // 高斯模糊背景组件
│   │
│   └── screens/                     // 对应的五大核心页面 (模仿 Apple Music 底部 Tab)
│       ├── listennow/               // "现在就听" (主页推荐)
│       │   ├── ListenNowScreen.kt
│       │   └── ListenNowViewModel.kt
│       ├── browse/                  // "浏览" (新歌、排行榜)
│       ├── radio/                   // "广播" (私人 FM 等)
│       ├── library/                 // "资料库" (用户的歌单、收藏)
│       ├── search/                  // "搜索"
│       ├── auth/                    // 登录域 UI
│       │   ├── AuthViewModel.kt
│       │   ├── PhoneLoginScreen.kt
│       │   ├── EmailLoginScreen.kt
│       │   ├── QrLoginScreen.kt
│       │   └── CaptchaLoginScreen.kt
│       │
│       └── nowplaying/              // 全屏播放界面 (非常关键！)
│           ├── NowPlayingScreen.kt  // 仿 Apple Music 的动态歌词、毛玻璃背景播放页
│           ├── LyricsView.kt        // 滚动歌词组件
│           └── NowPlayingViewModel.kt
│
└── util/                            // 工具类
    ├── Extensions.kt                // Kotlin 扩展函数
    ├── Constants.kt                 // 常量 (如 API_BASE_URL)
    └── TimeUtils.kt                 // 毫秒时间格式化等
```

### 开发建议与重点：

1. **对接 `api-enhanced` 时的注意事项**：
    * 网易云 API 需要处理登录态，建议使用 `Cookie` 或者 `realIP` 处理。在 `data/api/AuthInterceptor.kt` 中统一拦截并加上需要的凭证。
    * 音源 URL 获取：播放时需要通过 `/song/url/v1` 接口实时获取真实的 mp3/flac 链接喂给 Media3 Player。

2. **实现 Apple Music 的 UI 灵魂**：
    * **大标题排版 (Large Typography)**：在页面的顶部使用超大的粗体标题，向下滚动时可以平滑过渡到顶部的 TopBar。
    * **高斯模糊 (Glassmorphism)**：Apple Music 播放界面的灵魂。在 Jetpack Compose 中，可以使用 `RenderEffect.createBlurEffect` (Android 12+) 或者借助第三方库来实现背景随着专辑封面颜色动态变化的实时高斯模糊效果。
    * **动态圆角和动画**：展开底部迷你播放器 (Mini Player) 到全屏播放页面的手势驱动动画，可以使用 Compose 的 `ModalBottomSheet` 或自定义交互实现。

3. **后台播放体验**：
    * 千万不要用旧版的 `MediaPlayer`。一定要使用 **AndroidX Media3** 的 `MediaSessionService` 或 `MediaLibraryService`。它能完美兼容 Android 的后台限制、蓝牙耳机控制、锁屏播放控制以及车载 Android Auto。

4. **登录与会话管理（新增）**：
    * 登录能力建议独立为 `auth` 纵切模块，不和搜索/播放逻辑耦合。
    * 用 `AuthInterceptor` 统一追加 cookie（重点是 `MUSIC_U`），并在 401/登录失效时触发刷新策略。
    * `SessionLocalDataSource` 只负责存取，不做业务分支；业务分支统一放 `AuthRepository` + `UseCase`。
    * 二维码登录采用状态机轮询：`801 等待扫码` -> `802 待确认` -> `803 成功并落 Cookie`，`800` 过期后重建。
    * 登录接口避免高频调用，增加最小间隔/退避重试，防止风控。

### 登录功能架构改造（对应当前项目）

#### 1) 分层职责（必须先统一）
* `data/api/auth/AuthService`：仅声明接口，不含业务逻辑。
* `data/local/session/*`：管理 cookie 持久化与读取。
* `data/repository/AuthRepositoryImpl`：组合远端与本地，输出统一登录态。
* `domain/usecase/auth/*`：编排登录流程（验证码、二维码轮询、刷新、游客兜底）。
* `presentation/screens/auth/*`：只负责 UI 状态与用户交互。

#### 2) 关键函数清单（先定义接口，再写实现）
* `AuthService`
  * `loginByCellphone(...)`
  * `loginByEmail(...)`
  * `sendCaptcha(...)`
  * `verifyCaptcha(...)`
  * `createQrKey(...)`
  * `createQr(...)`
  * `checkQr(...)`
  * `guestLogin()`
  * `refreshLogin()`
* `AuthRepository`
  * `loginByPhone(...)`
  * `loginByEmail(...)`
  * `loginByCaptcha(...)`
  * `startQrLogin()`
  * `pollQrStatus(key)`
  * `guestLogin()`
  * `refreshSessionIfNeeded()`
  * `observeAuthState()`
* `SessionLocalDataSource`
  * `saveCookie(cookie: String)`
  * `getCookie(): String?`
  * `clearSession()`

#### 3) 推荐的落地顺序（MVP）
1. **MVP-A（会话地基）**：先打通 `CookieStore -> AuthInterceptor -> 请求自动带 cookie`。
2. **MVP-B（可登录）**：手机号/邮箱/验证码三条链路先做通，支持错误提示和重试。
3. **MVP-C（二维码）**：接 `key/create/check`，实现轮询、超时、过期重建。
4. **MVP-D（稳态）**：增加 `refreshLogin` 与 `guestLogin` 兜底，避免全局 400/未登录失败。
5. **MVP-E（体验）**：把登录态接入搜索/播放入口，未登录场景给出明确引导。

#### 4) 关键约束（避免踩坑）
* 不要在每次请求前主动重新登录；先读本地会话。
* 登录慢是正常现象（加密链路），UI 要有 loading + cancel。
* 对密码参数建议 URL 编码或 POST body 传输，避免特殊字符截断。
* 二维码轮询必须带时间戳，防止缓存。

### 目前的开发计划
第 1 步“搜索可用”（最优先）
目标：输入关键词，能拿到歌曲列表并显示基本信息（歌名/歌手/封面/id）。
主要文件：app/src/main/java/com/example/seteasecloudmusic/data/api/NeteaseMusicService.kt、app/src/main/java/com/example/seteasecloudmusic/data/model/SongDao.kt、新增仓库实现（可放 data/repository/）。
完成标准（DoD）：固定关键词（比如“周杰伦”）请求稳定返回，UI 能显示列表，不崩溃。
自测方式：连续搜 3 次同关键词；断网时有错误提示；空结果时有空态提示。

做完第 1 步后，马上进入第 2 步“点击可播放”
目标：从列表点一首歌，能获取播放 URL 并开始播放（先不追求完美 UI）。
主要产物：domain/model/Track、domain/usecase/*、player/playback/MusicPlayerController。
DoD：可播放/暂停/继续，异常 URL 不崩溃。
第 3 步才是后台播放（Media3）
目标：切后台不断播，通知栏/锁屏控制可用。
主要产物：player/playback/MusicService + app/src/main/AndroidManifest.xml 服务声明。
DoD：熄屏、切应用、耳机按键都能控。